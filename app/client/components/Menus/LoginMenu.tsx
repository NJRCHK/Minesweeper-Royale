import * as React from 'react'
import { AccountMenuProps } from '../../../shared/types'

export default function LoginMenuDiv(props: AccountMenuProps) {
    console.log(props);
    return (
        <div className='login-menu'>
            Login screen
        </div>
    )
}
